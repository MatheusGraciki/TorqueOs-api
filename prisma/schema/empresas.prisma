model Empresa {
  id        Int      @id @default(autoincrement())
  nome      String
  createdAt DateTime @default(now())

  usuarios Usuario[]
  clientes Cliente[]
  carros   Carro[]
  pecas    Peca[]
  servicos Servico[]

  @@map("empresas")
}
